pipeline {
    agent any
    tools {
        terraform 'Terraform'
    }
    stages {
        stage('Git Checkout') {
            steps {
                git credentialsId: 'GIT_CRED_siv', url: 'https://github.com/Cvaaaa/terraform.git'
            }
        }
        
    }
}
